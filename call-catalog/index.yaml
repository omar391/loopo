schemaVersion: fastflow/call-catalog-manifest/v3
pathTemplate: "{registry}/{kind}/{target}/{scope}/index.yaml"
prefixes:
  loopship:
    afn:
      service:
        child:
          tags: "loopship, child, worktree, quest"
        system:
          tags: "loopship, system, docs, signature"
        landing:
          tags: "loopship, landing, git, merge"
    workflow:
      service:
        flows:
          tags: "flow, flows, swe"
        step:
          tags: "step, archived, executing, landing, plan, questions, system-update, system, update, task-graph, task, graph, validation, verification"
